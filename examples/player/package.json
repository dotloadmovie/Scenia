{
  "name": "@as3-wasm-runtime/player-example",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone HTML page that loads a portable sketch.bundle.json via runtime-player.",
  "type": "module",
  "scripts": {
    "preview": "vite --host 0.0.0.0 --port 5175"
  },
  "devDependencies": {
    "vite": "^7.1.12"
  }
}
